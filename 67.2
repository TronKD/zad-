#include <iostream>
#include <fstream>
#include <cstdlib>
#include <math.h>

using namespace std; 

int fib(int n)
{
	if(n<3)
		return 1;
	
	return fib(n-2)+fib(n-1);
}

bool pierwsza(long long a) {
 	if (a==1) return false;
 	if (a==2 || a==3) return true;
 	for (long long p=2; p<=sqrt((double)a); p++){
 		if (a%p == 0) return false;
	}
 return true;
} 
 
int main(int argc, char** argv) {
	ofstream wyp;
	wyp.open("wyniki.txt");
	const int n = 40;
 	for (int i = 1; i <= n; i++) {
 		if(pierwsza(fib(i))){
		cout << "\n" << fib(i);
		wyp << "\n" << fib(i) ;
		}
	} 
    return 0;
} 
