#include <iostream>
#include <string>
#include <fstream>

using namespace std;

bool jednolity(string s){ 
	int d=s.length();
 	for(int i=1; i<d; i++)
 		if (s[i]!=s[i-1]) return false;
  return true;
} 

int main(int argc, char** argv) {
	ifstream File;
	File.open("dane_napisy.txt");
	ofstream wyp;
	wyp.open("wyniki_anagramy.txt");
	int ileZ1=0;
	string s1[1000], s2[1000];
 	for (int i=0; i<1000; i++)
 		if (jednolity(s1[i]))
 			if (s1[i]==s2[i]) ileZ1++;
			File.close(); 
 	cout << ileZ1 << endl;
	 wyp << ileZ1 << endl;	 
	return 0;
}
